import CustomSelect from "./index";

import { render } from "@testing-library/react";

describe("CustomSelect", () => {
  it("should renders correctly", () => {
    render(<CustomSelect type="Alguma coisa" setType={} />);
  });
});
